services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      # Persist SQLite database across container restarts
      - ./data:/app/data
    env_file:
      - backend/.env
    environment:
      - SQLALCHEMY_DATABASE_URL=sqlite:///./data/gokart.db
    restart: unless-stopped
